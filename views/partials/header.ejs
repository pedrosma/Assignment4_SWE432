<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= pageTitle || 'Campus Radio' %></title>
  <link rel="stylesheet" href="/css/base.css">
  <script defer src="/js/app.js"></script>
</head>
<body>
  <div class="layout">
    <header class="site-header">
      <div class="brand">
        <h1>Campus Radio</h1>
        <p>Node.js · EJS · MongoDB</p>
      </div>
      <nav class="topnav">
        <a href="/" class="<%= activePage === 'home' ? 'active' : '' %>">Home</a>
        <a href="/dj" class="<%= activePage === 'dj' ? 'active' : '' %>">DJ</a>
        <a href="/producer" class="<%= activePage === 'producer' ? 'active' : '' %>">Producer</a>
        <a href="/manager" class="<%= activePage === 'manager' ? 'active' : '' %>">Manager</a>
      </nav>
      <div class="actions">
        <button type="button" class="btn secondary" onclick="CampusRadio.toggleTheme()">Toggle Theme</button>
        <% if (isLoggedIn) { %>
          <form action="/auth/logout" method="POST">
            <button type="submit" class="btn">Logout</button>
          </form>
        <% } %>
      </div>
    </header>
    <main class="content">
